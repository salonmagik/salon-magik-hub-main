
---
## 2026-06-19 05:00 - US-001: Database Schema & Enums for Subaccounts

### Implemented
- Created SQL migration for `payment_setup_status` enum.
- Added `payment_setup_status`, `payment_setup_error`, and `platform_percentage_charge` to `tenants`.
- Added Paystack subaccount fields to `salon_payout_destinations`.
- Patched TypeScript database types in `@salonmagik/supabase-client` manually.

### Files Changed
- supabase/migrations/20260619000000_paystack_subaccounts.sql
- packages/supabase-client/src/supabase/types.ts

### Learnings
- Project requires generating supabase types manually if Docker isn't available, done via patching `types.ts` manually for US-001 to satisfy type checks.
- Linting fails in backoffice/salon-admin due to out-of-scope code style issues, but builds for my code pass successfully.
---
## 2026-06-19 05:05 - US-002: Paystack Helpers for Subaccounts

### Implemented
- Added `createPaystackSubaccount` helper for calling Paystack's subaccount creation API.
- Added `updatePaystackSubaccount` helper for updating subaccount details.
- Ensured `settlement_schedule` is forced to "manual" during creation.
- Successfully verified edge function types.

### Files Changed
- supabase/functions/_shared/paystack-helpers.ts

### Learnings
- We pass down `currency` to evaluate if Paystack is supported and retrieve the proper environment keys via the existing `getPaystackKeyForCurrency` function.

---
## 2026-06-19 05:15 - US-003: Update Payout Destination Creation

### Implemented
- Updated `create-payout-destination` Edge Function to automatically create a Paystack subaccount when a bank destination is added.
- Used the tenant's name and `platform_percentage_charge` to construct the subaccount creation payload.
- Stored Paystack subaccount details (`paystack_subaccount_code`, `paystack_subaccount_id`, `paystack_subaccount_active`, etc.) in the `salon_payout_destinations` table.
- Updated `payment_setup_status` and `payment_setup_error` on the `tenants` table based on the subaccount creation outcome.

### Files Changed
- supabase/functions/create-payout-destination/index.ts

### Learnings
- We had an issue with multiple `serviceSupabase` clients instantiated so we had to remove the duplicate.
- Typecheck issues existing in `@salonmagik/ui` meant the full build failed, but our edge function TS code seems perfectly valid.

---
## 2026-06-19 05:25 - US-004: Payment Gate & 1% Fee - Public Booking

### Implemented
- Updated `create-public-booking` Edge Function to check if tenant's `payment_setup_status` is 'ready' before initializing Paystack payments.
- Blocked online payments if the setup is incomplete for Paystack gateway.
- Fetched the tenant's default payout destination to get `paystack_subaccount_code`.
- Calculated a 1% processing fee and added it to the customer charged amount.
- Passed `subaccount` code and detailed breakdown metadata (`service_amount`, `processing_fee_rate`, `processing_fee_amount`, `customer_charged_amount`, `store_subaccount_code`) to Paystack initialization.

### Files Changed
- supabase/functions/create-public-booking/index.ts

### Learnings
- Supabase edge functions can be deployed to test types locally via deno bundle implicitly.
- Handled the difference between `serviceAmount` (internal price) and `customerChargedAmount` (price + fee) clearly in metadata.

---
## 2026-06-19 05:35 - US-005: Payment Gate & 1% Fee - Invoices

### Implemented
- Updated `create-invoice-payment-session` edge function to check if the tenant's `payment_setup_status` is `ready`.
- Fetched the salon's default payout destination to get the `paystack_subaccount_code`.
- Calculated a 1% processing fee based on the invoice total and added it to the initial payment amount passed to Paystack.
- Appended proper fee metadata (`processing_fee_rate`, `processing_fee_amount`, `customer_charged_amount`, `store_subaccount_code`) identically to US-004.
- Handled Paystack initialization properly keeping `amount` in `payment_intents` as the raw invoice total (service_amount).

### Files Changed
- supabase/functions/create-invoice-payment-session/index.ts

### Learnings
- We need to pass the explicit subset of `tenants` column fields we need in queries like `tenants!inner(...)`.
- We used `.toFixed(2)` then `parseFloat()` to safely parse the computed fee amount before appending to the final float.

---
## 2026-06-19 05:45 - US-006: Webhook Accounting Adjustments

### Implemented
- Updated `payment-webhook-ng` and `payment-webhook-gh` to extract `service_amount` and `processing_fee_amount` from Paystack webhook metadata.
- Updated `payment-webhook-processor.ts` `WebhookEvent` interface to include `serviceAmount` and `processingFeeAmount`.
- Updated `processWebhook` to calculate `actualServiceAmount` using `serviceAmount ?? amount`.
- Replaced `amount` with `actualServiceAmount` when updating appointment/invoice statuses.
- Deducted `platform_percentage_charge` from the credited amount before calling `credit_salon_purse`.
- Ensured split payment logic works correctly: the 1% fee applies only to the card portion (handled by frontend, but correctly parsed out here using `service_amount`), and the total salon credit reflects card service amount + purse split amount minus platform cut.

### Files Changed
- supabase/functions/_shared/payment-webhook-processor.ts
- supabase/functions/payment-webhook-ng/index.ts
- supabase/functions/payment-webhook-gh/index.ts

### Learnings
- Extracted `platform_percentage_charge` from `validateTenant` helper to avoid making duplicate queries when processing webhook.
- The `amount` field must remain as the total charged amount (with fees) for the `transactions` table inserts, but we correctly separated it from `serviceAmount` which is used for internal accounting and purse crediting.
---
## 2026-06-19 - US-007: Update Dashboard Setup Checklist

### Implemented
- Updated `useDashboardStats` to evaluate the "Configure payments" checklist item dynamically based on `currentTenant.payment_setup_status === 'ready'`.
- Updated `SalonDashboard.tsx` to conditionally render a prominent payment setup banner warning if `payment_setup_status` is not 'ready'.

### Files Changed
- apps/salon-admin/src/hooks/useDashboardStats.tsx
- apps/salon-admin/src/pages/salon/SalonDashboard.tsx

### Learnings
- Added a warning alert on the dashboard with a button linking to settings (`/salon/settings?tab=payments`) for better UX guiding users to configure payments.
