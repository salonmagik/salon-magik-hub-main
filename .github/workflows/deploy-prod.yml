name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: "Commit SHA to promote from staging"
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://app.salonmagik.com
    permissions:
      contents: write
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.commit_sha }}

      - uses: pnpm/action-setup@v4
        with:
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download staging promotion manifest
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: deploy-staging.yml
          commit: ${{ inputs.commit_sha }}
          name: promotion-manifest-staging
          path: artifacts

      - name: Validate staging promotion manifest
        run: |
          test -f artifacts/promotion-manifest-staging.json
          MANIFEST_SHA=$(jq -r '.commit_sha' artifacts/promotion-manifest-staging.json)
          MANIFEST_CI_RUN_ID=$(jq -r '.ci_run_id' artifacts/promotion-manifest-staging.json)
          MANIFEST_ARTIFACT_NAME=$(jq -r '.artifact_name' artifacts/promotion-manifest-staging.json)
          MANIFEST_SOURCE=$(jq -r '.source' artifacts/promotion-manifest-staging.json)
          MANIFEST_READY_FOR=$(jq -r '.ready_for' artifacts/promotion-manifest-staging.json)
          test "$MANIFEST_SHA" = "${{ inputs.commit_sha }}"
          test -n "$MANIFEST_CI_RUN_ID"
          test "$MANIFEST_CI_RUN_ID" != "null"
          test -n "$MANIFEST_ARTIFACT_NAME"
          test "$MANIFEST_ARTIFACT_NAME" != "null"
          test "$MANIFEST_SOURCE" = "staging"
          test "$MANIFEST_READY_FOR" = "prod"
          echo "CI_RUN_ID=$MANIFEST_CI_RUN_ID" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=$MANIFEST_ARTIFACT_NAME" >> "$GITHUB_ENV"

      - name: Download immutable build artifact from CI
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: ci.yml
          run_id: ${{ env.CI_RUN_ID }}
          name: ${{ env.ARTIFACT_NAME }}
          path: artifacts

      - name: Verify immutable build artifact
        run: |
          test -f artifacts/${{ env.ARTIFACT_NAME }}.tar.gz
          mkdir -p promoted-build
          tar -xzf artifacts/${{ env.ARTIFACT_NAME }}.tar.gz -C promoted-build

      - name: Apply DB migrations (prod)
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          for i in 1 2 3 4 5; do
            if supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_REF_PROD }}; then
              break
            fi
            if [ "$i" = "5" ]; then echo "supabase link failed after 5 attempts" && exit 1; fi
            echo "Attempt $i failed — retrying in 30s..."
            sleep 30
          done
          supabase db push --include-all --yes

      - name: Deploy edge functions (prod)
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy --project-ref ${{ secrets.SUPABASE_PROJECT_REF_PROD }}

      - name: Post-deploy smoke checks
        run: pnpm test

      - name: Advance release/prod to deployed SHA
        env:
          GH_PROD_PUSH_TOKEN: ${{ secrets.GH_PROD_PUSH_TOKEN }}
        run: |
          git remote set-url origin https://x-access-token:${GH_PROD_PUSH_TOKEN}@github.com/${{ github.repository }}.git
          git push --force origin ${{ inputs.commit_sha }}:refs/heads/release/prod
